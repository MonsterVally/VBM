vbm_loot_boxes_decisions = {
	vbm_loot_box_minors = {
		icon = decision_generic_nationalism
		available = {
			is_ai = no
		}
		visible = {
			NOT = {
				has_game_rule = {
					rule = vbm_allow_loot_boxes
					option = VBM_ALLOW_LOOT_BOXES__NO
				}
			}
		}
		cost = 100
		days_remove = 0
		ai_will_do = {
			factor = 0
		}
		complete_effect = {
			country_event = {
				id = vbm_loot_boxes.1
			}
		}
	}
	vbm_loot_box_minors_odds = {
		icon = decision_generic_nationalism
		available = {
			always = no
		}
		visible = {
			NOT = {
				has_game_rule = {
					rule = vbm_allow_loot_boxes
					option = VBM_ALLOW_LOOT_BOXES__NO
				}
			}
		}
		days_remove = 0
		ai_will_do = {
			factor = 0
		}
		complete_effect = {
			vbm_loot_box_minor_rewards_effect = yes
		}
	}
}
